# 🍹 GSAP Cocktails Landing Page

Сучасний лендінг-пейдж для бару коктейлів, створений на основі уроку з YouTube з використанням React, GSAP анімацій та Tailwind CSS.

## 📚 Урок

**Відео:** [Build a GSAP Cocktails Landing Page](https://www.youtube.com/watch?v=AW1yfBKRMKc&t=3100s)

Цей проект створено під час проходження туторіалу, де детально розглядаються сучасні техніки веб-анімації з використанням GSAP (GreenSock Animation Platform).

## 🚀 Технології

- **React 19** — бібліотека для створення інтерфейсів
- **Vite 7** — швидкий build tool
- **GSAP 3** — потужна бібліотека для створення анімацій
  - `@gsap/react` — React-інтеграція
  - `SplitText` — плагін для анімації тексту по символах/словах
  - `ScrollTrigger` — анімації при скролі
- **Tailwind CSS 4** — utility-first CSS framework
- **react-responsive** — адаптивні медіа-запити в React

## 📁 Структура проєкту

```
gsap_landing/
├── src/
│   ├── components/
│   │   ├── Hero.jsx       # Головна секція з відео та анімаціями
│   │   ├── Menu.jsx       # Слайдер з коктейлями
│   │   ├── About.jsx      # Секція "Про нас"
│   │   ├── Art.jsx        # Секція з майстерністю
│   │   ├── Cocktails.jsx  # Список коктейлів
│   │   ├── Contact.jsx    # Контактна інформація
│   │   └── Navbar.jsx     # Навігаційне меню
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── constants/
│   └── index.js           # Константи та дані (меню, коктейлі, контакти)
├── public/
│   ├── images/            # Зображення
│   ├── videos/            # Відео для героя
│   └── fonts/             # Шрифти
└── package.json
```

## 🎨 Реалізовані фічі

### Hero-секція (`Hero.jsx`)
- **Анімація тексту** через `SplitText` — кожен символ заголовка "MOJITO" анімується окремо
- **Parallax-ефект** для листочків при скролі (ScrollTrigger)
- **Відео-анімація** синхронізована зі скролом (currentTime прив'язано до позиції скролу)
- **Адаптивність** з використанням `useMediaQuery` для різних налаштувань на мобільних пристроях

### Menu-секція (`Menu.jsx`)
- **Інтерактивний слайдер** коктейлів з анімацією переходів
- **GSAP-анімації** для зображень, заголовків та описів при зміні слайда
- **Навігація** через табси або стрілки (попередній/наступний коктейль)
- **Динамічна зміна вмісту** з масиву даних `sliderLists`

### Інші компоненти
- `About.jsx` — секція з описом бару
- `Cocktails.jsx` — каталог напоїв
- `Art.jsx` — майстерність приготування
- `Contact.jsx` — контакти та години роботи
- `Navbar.jsx` — адаптивне меню навігації

## 🛠️ Що було виправлено під час розробки

### 1. `Hero.jsx`
**Проблема:** React-хуки (`useRef`, `useMediaQuery`) викликались всередині колбеку `useGSAP`, що порушувало Rules of Hooks.

**Рішення:**
- Винесено хуки на рівень компонента
- Зареєстровано GSAP-плагіни: `gsap.registerPlugin(SplitText, ScrollTrigger)`
- Додано захист роботи з `videoRef` через слухач `loadedmetadata`

### 2. `Menu.jsx`
**Проблема:** Імпорт неіснуючої змінної `allCocktails` (в `constants/index.js` експортується `sliderLists`).

**Рішення:**
- Змінено імпорт на `import { sliderLists as allCocktails } from '../../constants'`
- Виправлено некоректні значення `opacity: 100` → `opacity: 1` в GSAP-анімаціях

## 📦 Встановлення та запуск

```bash
# Клонувати репозиторій
git clone https://github.com/bfmvenom45/gsap-cocktails.git

# Перейти в директорію
cd gsap-cocktails

# Встановити залежності
npm install

# Запустити dev-сервер
npm run dev

# Зібрати продакшн-версію
npm run build

# Попередній перегляд білда
npm run preview
```

## 🌐 Деплой

Проєкт може бути задеплоєний на:
- **Vercel** (рекомендовано для Vite/React)
- **Netlify**
- **GitHub Pages**

```bash
npm run build
# Завантажити папку dist/ на хостинг
```

## 📝 Скрипти

- `npm run dev` — запуск локального сервера розробки
- `npm run build` — збірка для продакшну
- `npm run preview` — попередній перегляд білда
- `npm run lint` — перевірка коду через ESLint

## 🎯 Основні концепції з уроку

1. **GSAP Timelines** — створення послідовних анімацій
2. **ScrollTrigger** — прив'язка анімацій до скролу сторінки
3. **SplitText** — розбиття тексту на символи/слова для анімації
4. **useGSAP hook** — правильна інтеграція GSAP у React-компоненти
5. **Responsive animations** — адаптація анімацій під різні розміри екранів
6. **Video scrubbing** — синхронізація відео зі скролом через `currentTime`

## 🤝 Контакти

**GitHub:** [bfmvenom45](https://github.com/bfmvenom45)  
**Репозиторій:** [gsap-cocktails](https://github.com/bfmvenom45/gsap-cocktails)

## 📄 Ліцензія

Цей проєкт створено в навчальних цілях на основі туторіалу.